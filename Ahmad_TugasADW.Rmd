---
title: "Tugas 1"
author: "Ahmad muwafiqul adli"
date: "2025-04-21"
output: pdf_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```
```{r}
#MEMBACA DATASETS NILE
library(help = "datasets")
library(datasets)
help('datasets')
plot(Nile,xlab='Tahun',ylab='Nile',col='red')
# Melihat Struktur Dataset 
str(Nile)
#Melihat ringkasan dataset
summary(Nile)
# Plot time series
plot(Nile, main = "Time Series Nile", xlab = "Tahun", ylab = "Tinggi")
#Tren
#Data aliran Sungai Nil menunjukkan pola yang berubah sepanjang waktu. Terdapat indikasi penurunan volume aliran secara keseluruhan di periode tertentu, meskipun setelah tahun-tahun awal ada stabilisasi jangka panjang dalam kisaran tertentu. Tren ini mencerminkan perubahan lingkungan atau intervensi manusia.
#Seasonal
#Karena dataset ini adalah data tahunan, komponen musiman tidak terlihat secara eksplisit dalam analisis. Pola musiman lebih sering ditemukan dalam data bulanan atau mingguan dengan variasi periodik seperti perubahan curah hujan musiman.
#Random
#Fluktuasi pada data yang tidak dapat dijelaskan oleh tren atau pola musiman disebut komponen acak (random). Tahun-tahun dengan nilai ekstrem, seperti aliran yang sangat rendah atau tinggi, kemungkinan besar disebabkan oleh faktor-faktor unik seperti banjir besar, kekeringan, atau kejadian tak terduga lainnya.

```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r}
acf(Nile, main = "Autokorelasi Tinggi Sungai Nil")
#Fungsi acf()
#Fungsi acf() adalah singkatan dari "Autocorrelation Function" yang digunakan untuk menghitung dan memvisualisasikan autokorelasi dari sebuah time series.
# digunakan untuk mengidentifikasi pola temporal, seperti tren atau siklus, dengan melihat sejauh mana nilai masa lalu memengaruhi nilai saat ini.
#
```

## Including Plots

You can also embed plots, for example:
```{r}
#NOMOR 2 DATASET AIRPASSENGERS
library(datasets)
help("dataset")
plot(Nile,xlab='Tahun',ylab='Kedalaman Sungai Nile')

# Spesifikasi
# Data bawaan
data(Nile)

# Membuat vektor angka tahun (1871-1970)
tahun <- rep(c(1871:1970), length.out = length(Nile))
# Plot time series
plot(Nile, type = "o", pch = 20, xlab = "Tahun", ylab = "Tinggi Sungai Nil")
# Tambahkan label tahun di setiap titik
text(tahun, Nile, labels = tahun, pos = 3, cex = 0.6)


```

```{r}
#mengakses pustaka datasets
  library(datasets)
#memeriksa bantuan untuk dataset 
help('datasets')
#membuat plot ADW
plot(Nile,xlab='Tahun',ylab='Tinggi Sungai Nill')
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
